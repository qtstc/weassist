<UserControl x:Class="CitySafe.OverLay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    d:DesignHeight="800" d:DesignWidth="800"  >

    <Grid x:Name="LayoutRoot" Opacity="1" Background="Black" >
        <Grid.RowDefinitions>
            <RowDefinition Height="800"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ProgressBar IsIndeterminate="True" Foreground="{StaticResource PhoneAccentBrush}" Height="50" Width="480" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock x:Name ="Overlay_Textblock" Foreground="{StaticResource PhoneTextBoxBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>